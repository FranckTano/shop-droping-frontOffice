## Shop Dropping Front

Angular storefront and landing pages for the Shop Dropping project.

## Scripts

```bash
npm run start
npm run build
```

## Notes

- The Angular app starts from `src/main.ts`.
- Routes are defined in `src/app/app.routes.ts`.
- The storefront page is the default route at `/`.
